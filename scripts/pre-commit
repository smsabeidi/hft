#!/bin/sh
# Fast pre-commit gate: unit tests + cost-model invariants (~6s).
# Full walk-forward runs are NEVER a commit blocker (design doc, Distribution
# Plan) — multi-hour hooks get bypassed, fast hooks get obeyed.
set -e
cd "$(git rev-parse --show-toplevel)"
python3 -m pytest -q
